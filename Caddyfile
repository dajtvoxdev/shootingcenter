shootingcenter.click {
    # Handle API requests first
    handle_path /api/* {
        reverse_proxy backend:3001
    }
    
    # Everything else goes to frontend
    reverse_proxy frontend:80
}

www.shootingcenter.click {
    redir https://shootingcenter.click{uri}
}
